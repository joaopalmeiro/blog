---
import { basename } from "node:path";

import { posts } from "../data/posts";
import Layout from "../layouts/Base.astro";

const id = basename(import.meta.url, ".astro");
const post = posts.find((p) => p.id === id);
---

<Layout title="TODO" description="TODO">
  <header class="mb-12">
    <!-- TODO -->
  </header>

  <main>
    <article>
      <header>
        <h1 class="font-medium">{post?.title}</h1>

        <p>
          Published on <time datetime={post?.createdAt}>{post?.createdAt}</time>. Last updated on <time
            datetime={post?.lastUpdatedAt}>{post?.lastUpdatedAt}</time
          >
        </p>
      </header>
      <p>TODO</p>
    </article>
  </main>
</Layout>
